# PlaceLink 작업 결과 및 다음 할 일 — 2026-07-29

## 진행 상황 요약

- 전체 기능 개발 및 기존 Production 배포: 완료
- 분석 이벤트 조회 API·내부 대시보드: 구현 완료
- 명예의 전당 최근 7일 집계: 구현 완료
- 성수 승인 15곳 안전 목록·dry-run: 완료
- 성수 15곳 개발용 Supabase 쓰기·검증: 완료
- 연남·망원 액티비티 검색 개선: 완료
- 서촌 산책 후보 정리: 완료
- 한남 17곳 개별 검토: 완료
- OAuth 설정: 위 작업 완료 후 진행하도록 보류
- `place-link.com` 연결: 사용자 요청으로 추후 진행

## 현재 체크포인트

- Git 기준 브랜치: `main`
- 작업 시작 HEAD: `1e3654f` — `feat: add analytics hall of fame and feed filters (#2)`
- 기존 Production 주소:
  [https://placelink-umber.vercel.app/ko](https://placelink-umber.vercel.app/ko)
- GitHub PR: [#3](https://github.com/whitederrick/placelink/pull/3) 병합 완료
- Production 반영 커밋: `4056a64` — `feat: add weekly analytics operations (#3)`
- 운영 DB를 별도로 분리하지 않았으며 Vercel Production target과 연결된 Supabase는
  개발·검증 용도다.

## 이번에 완료한 작업

### 1. 성수 승인 목록 안전장치

- `seongsu-2026-07-29` 승인 목록에 승인된 15곳을 카테고리별로 고정했다.
- DB 쓰기 명령은 `--approval-set` 없이는 실행되지 않는다.
- 승인 장소가 Kakao 검색 결과에서 빠지면 일부만 쓰지 않고 전체 실행을 중단한다.
- dry-run은 DB 연결 없이 실행되며 15곳이 정확히 선택되는 것을 확인했다.
- 보류 2곳과 제외 1곳은 승인 목록에 포함하지 않았다.
- 개발용 Supabase에 실제 동기화해 15곳을 새로 생성했다.
- 읽기 전용 사후 검증 결과:
  - 활성 장소 15곳
  - Kakao 제공자 참조 15개
  - 카테고리: 바 3, 식당 3, 숍 3, 카페 3, 액티비티 1, 전시 2
  - 보류·제외 후보 0곳
- 동기화에 사용한 임시 원격 `DATABASE_URL` 파일은 검증 직후 삭제했다.

실행 예정 명령:

```bash
pnpm db:sync:kakao -- --area=seongsu --approval-set=seongsu-2026-07-29
```

### 2. 분석 이벤트 조회·모니터링

- 관리자 전용 `GET /api/v1/admin/analytics/summary`를 추가했다.
- 조회 기간은 7일 또는 30일이며 다음 정보를 제공한다.
  - 현재 기간·이전 기간 전체 이벤트
  - 로그인 사용자 이벤트
  - 이벤트 이름별 건수
  - 필터 사용 횟수와 마지막 사용 시각
  - 최근 이벤트 20건
  - 정상·미수집·24시간 이상 정체 상태
- 내부 화면은 `/{locale}/ops/analytics`다.
- `ADMIN_USER_IDS` 환경변수에 등록된 사용자만 API와 화면에 접근할 수 있다.
- 화면은 이벤트 속성이나 개인정보 원문을 노출하지 않는다.

### 3. 최근 7일 명예의 전당

- 누적 `스크랩 × 5 + 조회` 점수를 제거했다.
- 최근 7일 동안 생성된 실제 스크랩을 코스별로 집계한다.
- 점수는 `최근 7일 스크랩 × 5`이며 상위 3개 코스를 표시한다.
- 해체 커플과 삭제·비공개 코스는 기존처럼 제외한다.
- 최근 스크랩이 전혀 없는 초기 환경에서는 최신 공개 코스를 점수 0으로 표시한다.

### 4. 카카오 지역 후보 재검토

- 연남:
  - 액티비티 검색어를 `경의선숲길`로 변경했다.
  - 같은 이름 및 괄호 안 방향만 다른 후보를 중복 제거한다.
  - 17곳 중 승인 권고 16곳, 보류 1곳이다.
- 망원:
  - 액티비티 검색어를 `산책`으로 변경했다.
  - 망원정 터, 홍제천, 평화의공원 난지연못을 확보했다.
  - 17곳 중 승인 권고 16곳, 보류 1곳이다.
- 서촌:
  - 진경산수화길만 승인 권고했다.
  - 덕수궁산책길과 삼청공원 순환산책길은 권역 밖 후보로 제외 권고했다.
  - 18곳 중 승인 권고 16곳, 제외 2곳이다.
- 한남:
  - 17곳을 카테고리·거리 기준으로 개별 검토했다.
  - 17곳 모두 승인 권고다.

상세 목록:
[kakao-place-review-2026-07-29.md](./kakao-place-review-2026-07-29.md)

## 검증 결과

- 성수 승인 목록 dry-run: 15곳
- 연남·서촌·한남·망원 재검토 dry-run: 완료
- TypeScript:
  - Web 통과
  - Database 통과
- ESLint:
  - Web 통과
  - Database 통과
- Vitest:
  - Web 40개 통과
  - Database 6개 통과
- Next.js Production build 통과
- GitHub main CI:
  - Quality 통과
  - E2E 통과
- Vercel Production 홈 HTTP 200 및 최근 7일 문구 확인
- 관리자 분석 API 비인증 요청 HTTP 401 확인
- 개발용 Supabase 성수 Kakao 장소 15곳·활성 15곳·제외 후보 0곳 확인

## 남은 할 일

### 운영 설정

1. 운영 관리자 사용자 ID를 `ADMIN_USER_IDS`에 등록한다.
2. 관리자 로그인 후 `/{locale}/ops/analytics` 화면을 최종 확인한다.

### 별도 사용자 승인 필요

1. 연남 16곳 승인 권고안
2. 망원 16곳 승인 권고안
3. 서촌 16곳 승인 권고안
4. 한남 17곳 승인 권고안

### 마지막 단계로 보류

1. Kakao OAuth 콘솔 설정과 실제 콜백 URI 등록
2. Google OAuth 콘솔 설정과 실제 콜백 URI 등록
3. `place-link.com` DNS 연결은 이번 작업 범위에서 제외하고 추후 진행

## 환경 및 안전 메모

- Supabase 프로젝트는 개발·검증 전용이다.
- 로컬 DB는 Docker `placelink-postgres`, `localhost:54329`를 사용한다.
- 비밀값, OAuth 자격 증명, Supabase 비밀번호, Kakao 키는 문서나 Git에 기록하지
  않는다.
- 카카오 장소 동기화는 지역별 dry-run → 사람 검토 → 명시적 승인 → 승인 목록
  지정 → 개발 DB 반영 순서로만 진행한다.
- Vercel 환경을 확인하며 만든 임시 환경·인증 파일은 검증 직후 삭제했다.

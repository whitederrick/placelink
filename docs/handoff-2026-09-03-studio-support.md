# PlaceLink Studio 문의·불만·신고 관리 인수인계 — 2026-09-03

## 결과

- 작업 브랜치: `codex/user-core-qa`
- Studio 2단계의 문의·불만·신고 목록·상세와 처리 기능을 구현했다.
- 로컬 개발 데이터베이스에 신규 마이그레이션을 적용했다.
- 원격 저장소 반영, 운영 데이터베이스 마이그레이션, 배포는 수행하지 않았다.

## 구현 내용

- `/{locale}/studio/support`
  - 문의, 불만, 신고, 개인정보 요청 유형 필터
  - 낮음, 보통, 높음, 긴급 우선순위 필터
  - 접수, 처리 중, 사용자 응답 대기, 해결, 종료 상태 필터
  - 제목·본문·접수자 정보 검색과 담당자 필터
  - 처리기한 경과 표시와 커서 기반 페이지네이션
- `/{locale}/studio/support/{id}`
  - 접수자, 대상 정보, 담당자, 상태, 우선순위, 처리기한 확인
  - 상태·우선순위·담당자·처리기한 변경
  - 운영자 답변 기록과 내부 메모 타임라인
- 관리자 전용 API
  - `GET /api/v1/admin/support-cases`
  - `GET`, `PATCH /api/v1/admin/support-cases/{id}`
  - `POST /api/v1/admin/support-cases/{id}/entries`
- `SupportCase`, `SupportCaseEntry` 데이터 모델과 관련 열거형을 추가했다.
- 목록과 상세 검증용 문의·신고 시드 데이터를 추가했다.

## 안정성과 감사

- 모든 상태 변경은 운영 사유를 필수로 입력하며 데이터 변경과 감사로그 저장을 한
  트랜잭션으로 처리한다.
- 답변 기록과 내부 메모 추가도 감사로그와 같은 트랜잭션으로 처리한다.
- 상세 화면에서 읽은 최신 변경 시각을 수정 요청에 포함해 다른 운영자의 변경을
  실수로 덮어쓰지 않도록 한다. 충돌 시 API는 `409`를 반환한다.
- 화면과 API는 기존 `ADMIN` 서버 권한 검사를 사용한다.

## 현재 제한

- `STAFF_REPLY`는 운영자가 보낸 답변의 기록이다. 실제 이메일·푸시·앱 알림 발송은
  아직 연결하지 않았다.
- 사용자용 접수 API와 스팸·요청 빈도 제한은 아직 구현하지 않았다.
- 역할별 세부 권한은 후속 작업이다.

## 검증

- 린트, TypeScript, Production 빌드 통과
- Database 단위 테스트 19개 통과
- Web 단위 테스트 96개 통과
- Playwright 전체 10개 통과
  - 신고 필터, 상세 진입, 처리 상태·긴급도·담당자 변경, 내부 메모와 관리자 API 확인
    포함
- 로컬 데이터베이스 마이그레이션 상태 동기화 확인

## 다음 작업

1. 운영자 역할별 세부 권한을 구현하고 사용자 상태 변경을 연결한다.
2. 사용자용 문의 접수 API에 요청 빈도 제한과 개인정보 최소 수집 정책을 적용한다.
3. 답변 전달용 이메일·푸시 어댑터와 발송 상태·실패 재처리를 연결한다.
4. 장소·행사·코스 목록·상세와 신고 대상 콘텐츠 조치를 연결한다.

## 후속 완료 — 감사로그 조회

- `/{locale}/studio/audit-logs`에서 작업명·실행 주체·대상 ID를 검색하고 실행 주체와
  대상 유형으로 필터링할 수 있다.
- 변경 전후 JSON 스냅샷을 필요할 때 펼쳐 확인할 수 있다.
- `GET /api/v1/admin/audit-logs` 관리자 전용 API를 추가했다.
- 감사로그 조회는 읽기 전용이며 기존 `ADMIN` 권한 검사를 사용한다.
- 감사로그 서비스 단위 테스트와 화면·필터·API 브라우저 테스트를 통과했다.

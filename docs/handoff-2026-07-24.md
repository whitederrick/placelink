# PlaceLink 작업 인수인계 — 2026-07-24

## 현재 체크포인트

- 작업 브랜치: `feat/responsive-planner-ux`
- 로컬 HEAD: `5111551` — `feat: complete SEO foundations`
- 원격 기능 브랜치 HEAD: `3e6ba64` — `feat: add Kakao place sync pipeline`
- 로컬 브랜치는 원격보다 1개 커밋 앞서 있다. 이 문서 커밋까지 추가되면 2개 앞서게
  되며, Vercel 변수 값 입력 전에는 push하지 않는다.
- Draft PR #1: <https://github.com/whitederrick/placelink/pull/1>
  - 대상: `feat/responsive-planner-ux` → `main`
  - `Quality`: 통과
  - `E2E`: 통과
  - `Vercel`: 실패
- Vercel 실패 원인: 프로젝트에 유효한 `DATABASE_URL`이 없어 Prisma 설정 로드 단계에서
  빌드가 종료됐다.
- 사용자는 Vercel 변수 이름을 등록했으며 실제 값 입력은 진행 중이다. 값 자체는
  문서나 채팅에 남기지 않는다.
- 아직 완료되지 않은 외부 작업:
  - SEO 후속 커밋 원격 push
  - Vercel Preview 성공
  - PR ready 전환 및 `main` 병합
  - Production 배포
  - 운영 DB migration/seed 및 Kakao 실데이터 동기화

## 완료된 작업

- 비어 있던 `.git`을 `git init` 없이 원격 `whitederrick/placelink`의 메타데이터로 복구했다.
- 원격 저장소가 비어 있음을 확인하고 현재 프로젝트를 최초 커밋으로 보존했다.
- `main` 브랜치를 `origin/main`에 연결하고 다음 커밋을 푸시했다.
  - `907cccc` — `feat: initialize PlaceLink with CI gates`
  - `5602f89` — `fix: make CI gates portable`
- `.github/workflows/ci.yml`에 GitHub Actions CI를 구축했다.
  - `Quality`: PostGIS 시작 → Prisma migration deploy → `pnpm verify`
  - `E2E`: PostGIS 시작 → migration deploy → Prisma Client 생성 → Chromium 설치 → `pnpm test:e2e`
  - Playwright 보고서와 테스트 결과를 실패 시에도 아티팩트로 보존한다.
- Turborepo가 CI 환경 변수를 하위 작업에 전달하도록 `globalEnv`를 등록했다.
- CI에서는 Playwright 번들 Chromium을 사용하고 로컬에서는 설치된 Chrome을 재사용한다.
- GitHub ruleset `Protect main`을 활성화했다.
  - 기본 브랜치 `main`에 적용
  - `Quality`, `E2E`를 필수 GitHub Actions 상태 체크로 지정
  - 브랜치 삭제 제한 및 강제 푸시 차단
- 모바일 중심 서비스와 PC 코스 플래너를 함께 지원하도록 핵심 UI를 개편했다.
  - 760px 이상에서는 상단 내비게이션과 최대 1200px 작업 영역 사용
  - 탐색 화면은 지도와 장소 목록을 나란히 비교하는 분할 구조
  - 코스 편집은 현재 순서와 추천 장소를 동시에 보는 작업 공간
  - 마이 화면은 커플 기록, 통계, 작성·저장 코스를 넓은 그리드로 구성
  - 모바일에서는 하단 내비게이션과 단일 열 흐름 유지
- 네온 중심 색상을 따뜻한 아이보리·차콜·코랄 기반 디자인 체계로 교체했다.
- `앵커`, `동선`, `발행` 등 내부 용어를 `첫 장소`, `코스 순서`, `저장하고 공유`처럼 사용자 중심 표현으로 바꿨다.
- 390×844 모바일 화면의 가로 넘침과 핵심 CTA를 검증하는 Playwright 사용성 테스트를 추가했다.
- 반복되던 30개 개발 장소를 동네별 고유 이름과 분위기 설명으로 교체했다.
- 외부 업체 사진을 무단 사용하지 않도록 직접 생성한 서울 데이트 장소 이미지 아틀라스를 추가했다.
  - 전시, 카페, 편집숍, 식당, 산책, 리스닝 바의 6개 장면
  - 탐색 장소 카드와 홈 코스 카드에서 카테고리별로 잘라 사용
- 장소 이름에서 동네를 추측하지 않고 `areaSlug`를 기준으로 홈 카드 지역명을 표시하도록 수정했다.
- 초기 컴파일이 포함된 커플 E2E의 제한 시간을 60초에서 90초로 조정했다.
- 공식 Kakao Local REST API를 서버에서 호출하는 장소 공급자 어댑터를 추가했다.
  - 응답 스키마 검증, 10초 제한 시간, 최대 15개 크기 제한
  - REST API 키를 `Authorization: KakaoAK ...` 헤더로만 전달
  - 오류 메시지에 서버 키가 포함되지 않도록 단위 테스트
- 다섯 지역과 여섯 카테고리를 검색해 장소를 멱등 upsert하는
  `pnpm db:sync:kakao` 명령을 추가했다.
  - `--dry-run`, `--area=seongsu` 지원
  - Kakao 장소 ID를 `PlaceProviderRef`의 외부 고유 키로 사용
  - 사진·영업시간처럼 API 응답에 없는 정보는 생성하지 않음
- Kakao 앱 설정, 키 보관, dry-run, 반영 순서를 `docs/kakao-place-sync.md`에 문서화했다.
- GitHub Draft PR #1을 만들고 `Quality`, `E2E` 필수 체크 통과를 확인했다.
- 단계 2 SEO 완료 기준을 구현했다.
  - 공개 홈·탐색·발행 코스를 포함하는 자동 `sitemap.xml`
  - API·작성·마이·커플 초대 경로를 제외하는 `robots.txt`
  - ko/en canonical 및 hreflang 상호 참조
  - 코스 상세 Open Graph URL·이미지와 공통 metadata base
- Vercel 환경 변수, 마이그레이션, seed, Preview/Production 검증 순서를
  `docs/vercel-deployment.md`에 정리했다.

## 최종 검증

2026-07-24 기준:

- 로컬 `pnpm db:deploy`: 통과, 6개 마이그레이션 적용 상태
- 로컬 `pnpm verify`: 통과
  - lint, typecheck, 단위 테스트 37개, Next.js 프로덕션 빌드 통과
- 로컬 `pnpm test:e2e`: Playwright 3개 시나리오 통과
  - 커플 연결 생명주기
  - 코스 생성·저장·공유 생명주기
  - 390×844 모바일 홈·탐색·만들기 사용성 및 가로 넘침 검사
- GitHub Actions CI #2: 성공
  - `Quality`: 성공
  - `E2E`: 성공
  - 실행: <https://github.com/whitederrick/placelink/actions/runs/30060308652>
- 로컬 `main`과 `origin/main` 동기화 완료
- GitHub `Protect main` ruleset 생성 및 `main` 적용 상태 확인 완료
- Kakao 장소 어댑터 패키지 lint 및 typecheck 통과
- Kakao 요청·응답·키 비노출 단위 테스트 포함 데이터베이스 테스트 3개 통과
- SEO URL 해석 테스트 3개를 포함해 전체 단위 테스트 37개 통과
- `/robots.txt`, `/sitemap.xml`이 포함된 Next.js 프로덕션 빌드 통과
- PR #1의 GitHub Actions `Quality`, `E2E` 통과

## 남은 작업

1. Vercel Preview와 Production 변수에 실제 값을 입력하고 외부 Postgres를 연결한다.
   - 필수: `DATABASE_URL`, `AUTH_SECRET`
   - 실제 로그인에는 Kakao 또는 Google OAuth 변수 한 쌍 이상 필요
   - 로컬 Docker의 `localhost:54329` 주소는 Vercel에서 사용할 수 없음
2. 값 입력이 끝나면 Production DB에 마이그레이션을 적용한다.

   ```bash
   npx vercel env run -e production -- pnpm db:deploy
   ```

3. 운영 DB가 비어 있는지 먼저 확인하고, 빈 DB일 때만 seed를 실행한다.

   ```bash
   npx vercel env run -e production -- pnpm db:seed
   ```

4. 로컬 SEO·문서 커밋을 기능 브랜치에 push한다. push가 새 Preview 배포를 시작한다.
5. PR #1에서 `Quality`, `E2E`, `Vercel`이 모두 통과하는지 확인한다.
6. Preview의 `/ko`, `/en`, `/robots.txt`, `/sitemap.xml`, 로그인, 공개 코스 상세와
   Open Graph 이미지를 검증한다.
7. 검증 후 PR을 ready로 전환하고 `main` 병합 및 Production 배포를 진행한다.
8. Kakao REST API 키로 성수 한 지역을 dry-run하고 결과 검수 후 실제 DB 반영을
   승인한다.
   - 어댑터와 동기화 명령은 완료됐지만 아직 실호출하지 않았다.
   - Kakao Local 응답에 없는 사진과 영업시간은 별도 검증 소스가 필요하다.
9. 단계 2 제품 잔여 요구사항에서 다음 구현 항목을 선택한다.
   - 행동 이벤트 수집
   - 명예의 전당
   - 홈·피드·필터 제품 명세 정합성

## 주요 변경 파일

- `.github/workflows/ci.yml`
- `.gitattributes`
- `.gitignore`
- `package.json`
- `turbo.json`
- `apps/web/playwright.config.ts`
- `apps/web/src/app/redesign.css`
- `apps/web/src/components/AppShell.tsx`
- `apps/web/src/features/places/components/ExploreMapExperience.tsx`
- `apps/web/src/features/courses/components/RouteBuilderScreen.tsx`
- `apps/web/e2e/mobile-usability.spec.ts`
- `apps/web/public/images/places/seoul-date-atlas.png`
- `packages/database/src/seed-data.ts`
- `packages/database/src/kakao-local.ts`
- `packages/database/scripts/sync-kakao-places.ts`
- `docs/kakao-place-sync.md`
- `apps/web/src/app/robots.ts`
- `apps/web/src/app/sitemap.ts`
- `apps/web/src/lib/site-url.ts`
- `docs/vercel-deployment.md`

## 주의 사항

- 원격 저장소는 복구 시점에 기존 커밋이 없는 빈 저장소였다.
- `.agents`, `.pnpm-store`, 실제 `.env` 파일은 Git 추적에서 제외했다.
- CI의 배포 마이그레이션은 비대화형 `prisma migrate deploy`를 사용한다.
